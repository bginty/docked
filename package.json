{
  "name": "docked-static-site",
  "version": "1.0.0",
  "private": true,
  "description": "Static GitHub Pages storefront for Docked Cruise D2",
  "scripts": {
    "serve": "node scripts/serve-static.mjs",
    "validate": "node scripts/validate-static-site.mjs",
    "test": "node --test tests/static-site.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
